diff --git a/frontend/app/api/health/route.ts b/frontend/app/api/health/route.ts
index 00e6596..7271f82 100644
--- a/frontend/app/api/health/route.ts
+++ b/frontend/app/api/health/route.ts
@@ -3,10 +3,9 @@ import { NextResponse } from "next/server";
 export const dynamic = "force-dynamic";
 
 export async function GET() {
-  const apiUrl = process.env.NEXT_PUBLIC_API_URL || "http://localhost:4000/api/v1";
+  const apiUrl = process.env.BACKEND_INTERNAL_URL || "http://localhost:4000";
   try {
-    const base = apiUrl.replace(/\/api\/v1$/, "");
-    const res = await fetch(`${base}/api/health`, { next: { revalidate: 0 } });
+    const res = await fetch(`${apiUrl}/api/health`, { next: { revalidate: 0 } });
     const data = await res.json();
     return NextResponse.json({ frontend: "ok", backend: data });
   } catch {
diff --git a/frontend/next.config.mjs b/frontend/next.config.mjs
index 0418b83..e5faa96 100644
--- a/frontend/next.config.mjs
+++ b/frontend/next.config.mjs
@@ -28,7 +28,10 @@ const nextConfig = {
   },
 
   async rewrites() {
-    return [];
+    const backend = process.env.BACKEND_INTERNAL_URL || "http://localhost:4000";
+    return [
+      { source: "/api/:path*", destination: `${backend}/api/:path*` },
+    ];
   },
 
   async headers() {
diff --git a/package.json b/package.json
new file mode 100644
index 0000000..afe1898
--- /dev/null
+++ b/package.json
@@ -0,0 +1,11 @@
+{
+  "name": "bookmarketingos",
+  "private": true,
+  "scripts": {
+    "build": "npm ci --prefix backend && npm run build --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
+    "start": "concurrently -k -n backend,frontend \"PORT=4000 npm run start --prefix backend\" \"PORT=$PORT npm run start --prefix frontend\""
+  },
+  "devDependencies": {
+    "concurrently": "^8.2.2"
+  }
+}
